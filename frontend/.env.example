# Base URL of the deployed backend. Leave empty in local dev (Vite proxies /api
# and /socket.io to http://localhost:5000 — see vite.config.js).
#
# In production (Vercel) set this to the Railway backend URL, e.g.
# VITE_API_URL=https://instavs-backend.up.railway.app
VITE_API_URL=
